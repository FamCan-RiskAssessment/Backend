services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/famcan-riskassessment/backend:latest
    container_name: backend-RA
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M
    env_file:
      - ./.env
    ports:
      - ${SERVER_PORT}:${SERVER_PORT}
    depends_on:
      - db
    networks:
      - famcan-network

  db:
    image: postgres:17
    container_name: db-RA
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - ${DB_PORT}:5432
    volumes:
      - db-data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 2G
        reservations:
          cpus: "0.2"
          memory: 512M
    networks:
      - famcan-network

  rdb:
    image: redis:8
    container_name: rdb-RA
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - ${RDB_PORT}:6379
    volumes:
      - rdb-data:/data
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: 256M
        reservations:
          cpus: "0.1"
          memory: 64M
    networks:
      - famcan-network

volumes:
  db-data:
  rdb-data:

networks:
  famcan-network:
    external: true
